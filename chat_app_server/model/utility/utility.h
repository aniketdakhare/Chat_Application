#pragma once
#include <vector>
#include <string>
#include <iostream>
#include <bits/stdc++.h>

using namespace std;

struct Utility
{
    vector<string> split(const string &, string);
    string getCollectionName(string, string);
};